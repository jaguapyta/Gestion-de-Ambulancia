# backend/.env — DESARROLLO local (copiar a backend/.env y completar).
# NO subir el .env real a git.

PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=seme
JWT_SECRET=cambia_esto_en_desarrollo
JWT_EXPIRES_IN=8h
DATABASE_URL="mysql://root:@localhost:3306/seme"

# Origen(es) permitido(s) por CORS (coma-separados). En dev, el frontend:
CORS_ORIGIN=http://localhost:3000
